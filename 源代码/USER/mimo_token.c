#include "sys.h"
#include "usart2.h"
#include "usart3.h"

/**
 * @brief  MiMo Token??????
 * @param  buf ?????
 * @param  len ??????
 */
void MimoToken_HandleResponse(u8 *buf, u16 len)
{
    // ????:??MiMo????????Token
    if (len == 0 || buf == NULL) return;

    // 1. ??????(?????"TOKEN:"??)
    if (strstr((char *)buf, "TOKEN:") != NULL)
    {
        char *token = strtok((char *)buf, ":");
        token = strtok(NULL, "\r\n"); // ??Token???
        // 2. ??Token/??Token/??Token???
        // ...
    }
}